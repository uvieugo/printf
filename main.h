#ifndef _MAIN_H
#define _MAIN_H

#include <stdarg.h>
#include <stdio.h>

int _putchar(char);
int _printf(const char *format, ...);
int print_char(va_list arg_list);
int print_str(va_list arg_list);

#endif
